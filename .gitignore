# Compiled binary
/skiff
/dist/
*.exe

# A real app config (the repo ships skiff.example.toml instead)
/skiff.toml

# Real deploy configs with server addresses — never commit
skiff-*.toml
*.private.toml

# Node
node_modules/
.next/

# Env / secrets
.env
*.local

# OS
.DS_Store
