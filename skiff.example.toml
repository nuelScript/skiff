# Copy this to `skiff.toml` in your app's repo, then edit.

name = "myapp"

[build]
dockerfile = "Dockerfile"  # path to your Dockerfile
port       = 8080          # the port your app listens on

# Omit [server] to deploy to local Docker.
